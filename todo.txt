1) Add Dechirper Container to OMType and all relevant other elements, such as OMAppTemplate etc.
2) Change naming of module and class  (e.g. build.build())
3) Initialize the lattice with default values
4) generate energy profile at initialization